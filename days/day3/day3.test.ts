import { dayThreePartOne, dayThreePartTwo } from './day3';

describe('Day two tests', () => {
  test('Part one', () => {
    expect(dayThreePartOne()).toBe(157);
  });

  test('Part two', () => {
    expect(dayThreePartTwo()).toBe(70);
  });
});
